<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Redirect everything to index.php for API routes
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^api/(.*)$ api/index.php?path=$1 [QSA,L]
</IfModule>
